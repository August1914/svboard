import { Operation, Exchange } from '@urql/core';
export interface Options {
    shouldUpgrade?: (op: Operation) => boolean;
    ttl?: number;
}
export declare const requestPolicyExchange: (options: Options) => Exchange;
